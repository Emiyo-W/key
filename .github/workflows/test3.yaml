name: test3

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  backend-jobs:
    uses: ./.github/workflows/test.yaml@main

  frontend-jobs:
    uses: ./.github/workflows/test2.yaml@main